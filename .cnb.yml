$:
  vscode:
    - script:
        runner:
          cpus: 2
          memory: 2G
        services:
          - vscode
        stages:
          - name: "Start application"
            script:
              - chmod +x start_app.sh
              - bash start_app.sh
        ports:
          - "7860"
